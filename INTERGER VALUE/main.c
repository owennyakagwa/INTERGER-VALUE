#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number;

    printf("Enter an integer value: ");
    scanf("%d", &number);

    printf("The entered value is: %d\n", number);
    return 0;
}
